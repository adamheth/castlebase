#
#	Prettier Colors
#
#	Color Codes:
#-----------------------------
#	a = black
#	b = red
#	c = green
#	d = brown
#	e = blue
#	f = magenta
#	g = cyan
#	h = light gray
#	x = default
#
#==============================

export CLICOLOR=1
export LSCOLORS=fxcxgxgxBxgxgxgxgxFxFx

#
#	Aliases
#

#	Directory list
alias l='ls -lp'
alias ll='ls -alhF'

#
#	NoCorrect
#
alias node='nocorrect node'
alias ack='nocorrect ack'

#
#	Shortcuts
#
alias komodo='open -a Komodo\ Edit'
alias ko='komodo'


#
#	Load System Specific Aliases
#
if [ -f ~/.profile ]; then
	. ~/.profile
fi

#
#	FUNctions
#

# Don't want to type 'git' anymore

function log {
	if [[ -d .git ]]; then
		git log $@
	fi
}

function dif {
	if [[ -d .git ]]; then
		git diff $@
	fi
}

function ci {
	if [[ -d .git ]]; then
		git commit -m $@
	fi
}

#	Appended Aliases (added from the command line)
# ------------------------------------------------
alias h5='cd ~/Sites/html5-boilerplate/build; ./createproject.sh; cd ...'